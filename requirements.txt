# HTTP client (without aiodns to avoid pycares compatibility issues)
aiohttp>=3.10.0

# Environment variables
python-dotenv>=1.0.0

# NOTE: Do NOT install aiodns or pycares - they have compatibility issues
# that cause AttributeError during import. aiohttp works fine without them.
